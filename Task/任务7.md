任务7:

根据我给你的逻辑，计算并更新tax_tuishui_j表里的预扣金额、实缴金额、（预扣-实缴）金额、涉及税地、涉及商户、涉及渠道、涉及销售、涉及客服字段

页面增加退税着急名单一栏并增加三个按钮：
1. 导入退税着急名单
2. 计算退税着急名单
3. 查询退税着急名单

1.选择excel导入到表中tax_tuishui_j中，excel的A列对应姓名，B列对应身份证号，C列对应退税金额，表格式如sql/tax_tuishui_j.sql

2. 具体：计算2的逻辑如下：

关联条件: 
tax_tuishui_j与v_yukou_all是一对多的关系
tax_tuishui_j与yijiao_info是一对多的关系

tax_tuishui_j.id_card = v_yukou_all.id_card
tax_tuishui_j.id_card = yijiao_info.id_card
逻辑：

- 根据表tax_tuishui_j的id_card字段关联，查询视图v_yukou_all中的数据，计算并更新表tax_tuishui_j的预扣金额pre_deduct = 视图v_yukou_all的sum(tax_amount) 
- 根据表tax_tuishui_j的id_card字段关联，查询表yijiao_info中的数据，计算并更新表tax_tuishui_j的实缴金额actual_pay = 表yijiao_info的sum(paid_amount)
- 计算并更新表tax_tuishui_j的diff_amount = pre_deduct - actual_pay
- 更新tax_area字段为v_yukou_all表的tax_area字段，多个值用逗号隔开
- 更新merchant字段为v_yukou_all表的merchant字段，多个值用逗号隔开
- 更新channel字段为v_yukou_all表的channel字段，多个值用逗号隔开
- 更新sale字段为v_yukou_all表的sale字段，多个值用逗号隔开
- 更新customer字段为v_yukou_all表的customer_service字段，多个值用逗号隔开

2. 具体：点击‘查询退税着急名单’按钮，打开一个新页面，查询表tax_tuishui_j中的数据，显示在新页面上，需要提供分页，每页显示10条数据。
- 显示表tax_tuishui_j的姓名、身份证号、联系电话、扣缴义务人名称、退税金额、预扣金额、实缴金额、（预扣-实缴）金额、涉及税地、涉及商户、涉及渠道、涉及销售、涉及客服字段
- 增加对姓名，身份证号的模糊搜索功能
