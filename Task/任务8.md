任务8：
根据我给你的逻辑，计算并更新tax_tuishui表里的预扣金额、实缴金额、（预扣-实缴）金额、涉及税地、涉及商户、涉及渠道、涉及销售、涉及客服字段

页面增加退税全量名单一栏并增加两个个按钮：
1. 计算退税全量名单
2. 查询退税全量名单


1. 具体：计算的逻辑如下：
tax_tuishui_all目前是空表，每次计算需要先清空一下表，然后：
目标是要计算所有的人（预扣-实缴）金额，一个人就一条汇总数据，所以要取v_yukou_all表的id_card，然后根据id_card进行计算统计

关联条件: 
tax_tuishui_all与v_yukou_all是一对多的关系
tax_tuishui_all与yijiao_info是一对多的关系

tax_tuishui_all.id_card = v_yukou_all.id_card
tax_tuishui_all.id_card = yijiao_info.id_card
逻辑：

- 根据表tax_tuishui_all的id_card字段关联，查询视图v_yukou_all中的数据，计算并更新表tax_tuishui_all的预扣金额pre_deduct = 视图v_yukou_all的sum(tax_amount) 
- 根据表tax_tuishui_all的id_card字段关联，查询表yijiao_info中的数据，计算并更新表tax_tuishui_all的实缴金额actual_pay = 表yijiao_info的sum(paid_amount)
- 计算并更新表tax_tuishui_all的diff_amount = pre_deduct - actual_pay
- 更新tax_area字段为v_yukou_all表的tax_area字段，多个值用逗号隔开
- 更新merchant字段为v_yukou_all表的merchant字段，多个值用逗号隔开
- 更新channel字段为v_yukou_all表的channel字段，多个值用逗号隔开
- 更新sale字段为v_yukou_all表的sale字段，多个值用逗号隔开
- 更新customer字段为v_yukou_all表的customer_service字段，多个值用逗号隔开

2. 具体：点击‘查询退税全量名单’按钮，打开一个新页面，查询表tax_tuishui_all中的数据，显示在新页面上，需要提供分页，每页显示10条数据。
- 显示表tax_tuishui_all的姓名、身份证号、联系电话、预扣金额、实缴金额、（预扣-实缴）金额、涉及税地、涉及商户、涉及渠道、涉及销售、涉及客服字段
- 增加对姓名，身份证号的模糊搜索功能

考虑效率：
v_yukou_all数据量100万条，id_card去重大约20万条
yijiao_info数据量3万多条